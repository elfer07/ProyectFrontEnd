export class SocialNetwork {
    id: number;
    name: string;
    linkNetwork: string;

    constructor(id: number, name: string, linkNetwork: string) {
        this.id = id;
        this.name = name;
        this.linkNetwork = linkNetwork;
    }
}
